// Панель встроенной формы («Новая позиция» в сделке, «Новая отгрузка» и т. п.).
//
// Плотная заливка тут давит: панель тяжелее самой карточки, в которой лежит.
// Поэтому фон почти белый, а объём даёт текстура — редкая точечная сетка цвета
// бренда (как черновик на миллиметровке). Рамка остаётся пунктирной: блок
// временный, его заполняют и закрывают.
//
// Три `bg-*` не конфликтуют — это разные свойства: цвет, картинка и размер
// картинки. Точка = brand_main (#089E8D) с малой альфой.
export const INLINE_FORM_PANEL = [
    "rounded-lg border border-dashed border-brand_main/50 p-3",
    "bg-brand_main/[0.02]",
    "bg-[radial-gradient(rgba(8,158,141,0.16)_1px,transparent_1px)]",
    "bg-[length:14px_14px]",
].join(" ")
