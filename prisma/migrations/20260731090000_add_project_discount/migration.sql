-- Скидка проекта: наследуется от контрагента/группы при создании проекта и
-- дальше подставляется в сделки этого проекта. NULL = скидка не задана,
-- сделка берёт её из карточки клиента.
ALTER TABLE "projects" ADD COLUMN "discount" DECIMAL;
